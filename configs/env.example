# Copy this file to an ignored local env file before adding real values.
# Do not commit credentials or provider account details.

FOURBROPIX_ENV=local
FOURBROPIX_PROVIDER=dry_run
FOURBROPIX_LOG_LEVEL=info

# Optional local config paths. Keep these relative when possible.
FOURBROPIX_PATH_CONFIG=configs/paths.local.yaml
FOURBROPIX_MANIFEST=manifests/shot_manifest.example.json

# Provider credentials belong in ignored local files or a secret manager.
# Do not place real provider credential variables in this committed template.
